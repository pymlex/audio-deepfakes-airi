lr: 0.0001
epochs: 8
batch_size: 32
num_workers: 8
subset_size: 6000
val_fraction: 0.1
model_name: aasist_lite
loss_type: ce
use_augmentation: true
freeze_backbone: false
class_weight_alpha: 0.5
device: cuda
checkpoint_dir: checkpoints
output_dir: task1/outputs
wandb_project: audio-deepfakes-airi-cm
wandb_run_name: aasist_lite
seed: 42
